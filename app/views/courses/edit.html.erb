<div class="card">
	<div class="card-header">
	  <h2>Editing Course</h2>
	</div>
	<div class="card-body">
	  <%= render 'form', course: @course %>
	</div>
</div>

<%= link_to 'Show', @course %> |
<%= link_to 'Back', courses_path %>
